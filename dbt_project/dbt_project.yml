name: greenhouse
version: '1.0.0'
config-version: 2
profile: greenhouse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  greenhouse:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
